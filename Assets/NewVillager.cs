using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewVillager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*set a timer, wait and send him off on a search path
         if coneofvisibility proves true, send him off to the priest
         õnce the priest is notified via a collision box, set the priest off
         on a search path, with the first point being where the villager spotted the witch.

        once priest is there, and does not see the witch, send the priest off on a search path
        and have the NPC follow behind the priest with a pitchfork ;)
        */
    }
}
